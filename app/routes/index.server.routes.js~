

module.exports = function(app) {
    
    //route or path for the controller of the index page
	var index = require('../controllers/index.server.controller');
	
	//creates the index page url
	app.get('/', index.render);
};
